# Benchmarks

## Hardware

All benchmarks run on:

- **CPU**: TODO (e.g. AMD Ryzen 9 5900X — fill in `lscpu` / Task Manager → CPU)
- **RAM**: TODO
- **OS**: Windows 11 Home 10.0.26200
- **Rust toolchain**: TODO (run `rustc --version`)
- **No background load** during measurement runs

## Methodology

- All numbers are `criterion` wall-clock measurements over 1000+ iterations.
- Reported as p50 / p99 / p999. Averages are not reported.
- Benchmarks run with no other load on the machine.

## Results

| Benchmark | p50 | p99 | p999 |
|-----------|-----|-----|------|
| TODO      |     |     |      |
